# render.yaml - FINAL DOCKER VERSION

services:
  - type: web
    name: universal-downloader
    # Tell Render to look for a Dockerfile in your project
    runtime: docker
    plan: free
    # Health check for Render to know your app is running
    healthCheckPath: /
    # Auto-deploy when you push to GitHub
    autoDeploy: true